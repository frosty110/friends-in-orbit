package app.orbit.data.entity

enum class ListType { STATIC, SMART }

enum class CallDirection { OUTGOING, INCOMING }

enum class CallSource { CALL_LOG, MANUAL }

enum class RuleKind { KEEP_IN_TOUCH, LATE_NIGHT, ENERGIZE }
