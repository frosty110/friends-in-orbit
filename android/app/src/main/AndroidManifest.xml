<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Core functionality: auto-detect call history (PRD §Platform & Stack). -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- CORE-02 — Android 11+ package-visibility gate for ACTION_DIAL.
         Without this, Intent(ACTION_DIAL).resolveActivity() returns null
         even on devices with a dialer. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
    </queries>

    <application
        android:name=".OrbitApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Orbit">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Orbit.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Disable WorkManager's default initializer so the
          Configuration.Provider on OrbitApp takes effect (HiltWorkerFactory).
          Without this override, WorkManager initializes itself with the
          reflective default factory at first getInstance() call and the
          Hilt factory is never consulted — @HiltWorker instantiation would
          throw at runtime.

          This must land together with Configuration.Provider on OrbitApp:
          removing the default initializer without it crashes cold-launch.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!--
          WIDGET-01 — 2×2 "Next call" widget receiver.
          android:exported="false" — APPWIDGET_UPDATE is a protected system broadcast;
          third-party apps cannot trigger it (T-11-06 mitigation).
          FQN permanent post-release: do NOT rename OrbitWidget2x2Receiver.
        -->
        <receiver
            android:name=".widget.OrbitWidget2x2Receiver"
            android:exported="false"
            android:label="@string/widget_2x2_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_2x2" />
        </receiver>

        <!--
          4×2 "Call suggestions" widget receiver.
          android:exported="false" — APPWIDGET_UPDATE is a protected system broadcast;
          third-party apps cannot trigger it (T-11-13 mitigation).
          FQN permanent post-release: do NOT rename OrbitWidget4x2Receiver.
        -->
        <receiver
            android:name=".widget.OrbitWidget4x2Receiver"
            android:exported="false"
            android:label="@string/widget_4x2_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_4x2" />
        </receiver>
    </application>
</manifest>
