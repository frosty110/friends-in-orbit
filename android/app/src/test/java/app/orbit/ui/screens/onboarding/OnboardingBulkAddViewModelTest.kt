package app.orbit.ui.screens.onboarding

import org.junit.Ignore
import org.junit.Test
import kotlin.test.assertTrue

/**
 * **NEUTRALIZED STUB.**
 *
 * The previous body referenced `OnboardingBulkAddViewModel` /
 * `OnboardingBulkAddUiState` / `finishOnboarding` — onboarding components that
 * either never landed or were renamed. The test file was committed against
 * phantom classes, blocking `:app:compileDebugUnitTestKotlin`.
 *
 * Repairing the dead test in-shape is out of scope — the onboarding bulk-add
 * VM authoring is owned elsewhere. Same scope-bounded unblock as
 * `OnboardingCreateListViewModelTest` and `AppViewModelTest`.
 *
 * **Owner:** onboarding — when `OnboardingBulkAddViewModel` lands, this file
 * should be re-authored against the real surface (or deleted if the design
 * coalesced onto a different VM).
 *
 * Pre-existing — broken on HEAD before this stub. Same precedent as other
 * mechanical fixture repairs that unblock the verify gate.
 */
class OnboardingBulkAddViewModelTest {

    @Test
    @Ignore("Pre-existing dead test — neutralized. Onboarding owns the reauth.")
    fun `placeholder — onboarding reauthors against real OnboardingBulkAddViewModel surface`() {
        assertTrue(false, "Onboarding reauthors this against the real VM surface")
    }
}
